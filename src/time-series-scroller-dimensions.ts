import { IChartDimensions } from "./hp-time-series-chart/interfaces";

export interface ITimeSeriesScrollerDimensions extends IChartDimensions {
  sliderHeightPx: number;
  sliderHandleWidthPx: number;
}